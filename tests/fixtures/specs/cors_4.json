{
	"@comment": "Simple (non-preflight) cross-origin GET from an ALLOWLISTED origin: the grant is stamped on the real response too — echoed, credentialed, Vary:Origin. Pairs with cors_5, the same request from a foreign origin.",
	"in": {
		"method": "GET",
		"url": "http://localhost:8080/param_forwarding/some/foo/bar",
		"header": {
			"User-Agent": "some",
			"Origin": "http://localhost:3000"
		}
	},
	"out": {
		"status_code": 200,
		"header": {
			"content-type": ["application/json; charset=utf-8"],
			"Cache-Control": ["public, max-age=3600"],
			"X-Gateway-Completed": ["true"],
			"Access-Control-Allow-Origin": ["http://localhost:3000"],
			"Access-Control-Allow-Credentials": ["true"],
			"Vary": ["Origin"]
		}
	}
}

# Binary (root only)
/gateway

# Build artifacts
builder/skel/
vendor/

# Secrets
.env
.env.local
*.secret
*.pem
*.key
*.crt

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
ingress

# AI agent context files
AGENTS.md
GEMINI.md
QWEN.md
# Compiled admin-guard binary at repo root (from `go build ./cmd/admin-guard`).
# Anchored so it does NOT ignore the cmd/admin-guard/ source directory.
/admin-guard
